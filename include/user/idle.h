#ifndef __IDLE_H__
#define __IDLE_H__

/**
 * @brief The idle task which runs only when no other tasks are running.
 * @details The idle task which runs only when no other tasks are running.
 */
void idle_task(void);

#endif //__IDLE_H__
